[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
authors = [{name = "Manuel Holtgrewe"}]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]
dependencies = [
    "biopython>=1.86",
    "loguru>=0.7.3",
    "pycairo>=1.28.0",
    "pydantic>=2.12.4",
    "pysam>=0.23.3",
    "typer>=0.20.0",
]
description = "MUC1 VNTR analysis for short- and long-read sequencing data"
keywords = []
name = "mainz_muc1_vntr"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"

[project.scripts]
mainz-muc1-vntr = "mainz_muc1_vntr.__main__:main"

[project.urls]
Documentation = "https://github.com/medgen-mainz/mainz_muc1_vntr"
Issues = "https://github.com/medgen-mainz/mainz_muc1_vntr/issues"
Source = "https://github.com/medgen-mainz/mainz_muc1_vntr"

[tool.hatch.version]
path = "mainz_muc1_vntr/version.py"

[dependency-groups]
dev = [
    "black==25.9.0",
    "coverage>=7.10.7",
    "docutils>=0.18.1,<0.22",
    "flake8>=3.6.0",
    "hatch~=1.14.2",
    "pyright",
    "pytest-cache>=1.0",
    "pytest-cov",
    "pytest-cov>=2.4.0",
    "pytest-sugar>=0.8.0",
    "pytest>=3.0.6",
    "ruff>=0.14.4",
    "syrupy>=5.0.0",
]

[tool.hatch.envs.quality]
installer = "uv"
feature = [
  "dev",
]

[tool.hatch.envs.quality.scripts]
check = [
  "black --check --diff --preview src tests",
  "ruff check src tests",
]
format = [
  "black src tests",
  "ruff check --fix --unsafe-fixes src tests",
  "check",
]
typecheck = [
  """
  pyright --pythonpath="$(uv run --active hatch run quality:python -c 'import sys; print(sys.executable)')" \
    {args:src/ tests//}
  """
]

[tool.pyright]
include = ["src/mainz_muc1_vntr", "examples"]
exclude = [
    "**/node_modules",
    "**/__pycache__",
]
defineConstant = { DEBUG = true }
stubPath = "src/stubs"

reportMissingImports = "error"
reportMissingTypeStubs = false

pythonVersion = "3.13"
pythonPlatform = "Linux"

[tool.ruff]
lint.ignore = [
  "E501", # line too long, handled by black
  "B008", # do not perform function calls in argument defaults
  "C901", # too complex
  # the following are known issues in generated code
  "E721", # is vs. ==
  "C409", # Unnecessary list literal
  "B028", # No stacklevel in warnings.warn
  "B904", # raise from e not used
  "C405", # Unnecessary list literal (rewrite as set literal)
  "C402", # inconvenient dictionary creation
]
lint.select = [
  "E", # pycodestyle errors
  "W", # pycodestyle warnings
  "F", # pyflakes
  "I", # isort
  "C", # flake8-comprehensions
  "B", # flake8-bugbear
]

[tool.black]
line-length = 120

[tool.pytest.ini_options]
addopts = "--doctest-modules"

[tool.ruff.lint.isort]
known-first-party = ["src/mainz_muc1_vntr"]

[tool.hatch.envs.tests]
installer = "uv"
feature = [
  "dev",
]

[tool.hatch.envs.tests.scripts]
run = "pytest --cov=src/mainz_muc1_vntr --cov-report=term-missing --durations 5 -s tests/ src/mainz_muc1_vntr {args:tests}"
run-snapshot = "pytest --cov=src/mainz_muc1_vntr --cov-report=term-missing --durations 5 -s --snapshot-update tests/ src/mainz_muc1_vntr {args:tests}"

[tool.hatch.envs.docs]
installer = "uv"
feature = [
  "dev",
]

[tool.hatch.envs.docs.scripts]
build = "sphinx-build -b html docs docs/_build/html"
clean = "rm -rf docs/_build"
serve = "python -m http.server 8000 --directory docs/_build/html"

[tool.hatch.envs.build]
installer = "uv"
feature = [
  "dev",
]

[tool.hatch.envs.build.scripts]
run = "hatch build"

[tool.hatch.build.targets.wheel]
packages = ["src/mainz_muc1_vntr"]

[tool.hatch.build.targets.sdist]
packages = ["src/mainz_muc1_vntr"]
exclude = [
  "/.editorconfig",
  "/Makefile",
  "/.gitignore",
  "/.github",
  "/.vscode",
  "/.gitignore",
  "/tests",
]
