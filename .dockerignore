# The build only needs pyproject.toml, pixi.lock, README.md and src/. Everything else is
# excluded so a local `docker build` does not ship the repo, the LFS fixtures or a host
# pixi prefix into the build context.
*

!pyproject.toml
!pixi.lock
!README.md
!src/
